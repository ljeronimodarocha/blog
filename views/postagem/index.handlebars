<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <p><small>Data da publicação: {{postagem.data}}</small></p>
        <hr>
        <p>{{postagem.conteudo}}</p>
    </div>
</div>